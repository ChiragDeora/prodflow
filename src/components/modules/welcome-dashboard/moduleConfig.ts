import { Home } from 'lucide-react';
import { ModuleConfig } from '../moduleRegistry';

export const moduleConfig: ModuleConfig = {
  id: 'welcome-dashboard',
  label: 'Dashboard',
  icon: Home,
  description: 'Welcome dashboard and quick access'
};

